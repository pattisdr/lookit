import { include } from '../../../helpers/include';
import { module, test } from 'qunit';

module('Unit | Helper | include');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = include([42]);
  assert.ok(result);
});
